# Set to false to talk to the real PULSE backend (uvicorn api:app --port 8000).
# Keep it true only when api.py isn't running / you just want to preview the UI.
VITE_USE_MOCK=true
VITE_API_BASE_URL=http://localhost:8000

# Optional: point this at a remote LLM endpoint for the analyst Copilot panel.
# Leave blank to use the built-in local heuristic provider.
VITE_LLM_ENDPOINT=

# Supabase — used only for phone/password sign-in and the display-name
# profile row. Not part of the PULSE risk contract.
# Project: "pulse" under HenryTech12's Org (eu-west-1) — replaces the
# Bolt.new-provisioned project this frontend originally shipped with.
VITE_SUPABASE_URL=https://assipoqrioznbghvmeto.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImFzc2lwb3FyaW96bmJnaHZtZXRvIiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODQ0MDYxMTEsImV4cCI6MjA5OTk4MjExMX0.IIT-6s2tsk_Y8Hk-9AaYiOAfhgr6KTdM35B1-lEpBmg
